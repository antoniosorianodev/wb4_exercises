"use strict"

// created an array (list) of family members names
let myFamilyArray = ["Antonio", "Me", "Myself", "I"];

// get the number of items on the list (in this specific case it should be 4)
let numberOfFamilyMembers = myFamilyArray.length;

// loop over my family array and print the names
for (let i = 0; i < numberOfFamilyMembers; i++) {

    // print out each family member's name
    console.log(myFamilyArray[i]);

}